#!/usr/bin/env python3
# -*- coding: utf-8 -*-

def solve(a, b, c):
    len([(a,b,c) for a in range(1,11) for b in range(1,11) for c in range(1,11) if a+b+c == 24 if a**2+b**2 == c**2])
    for i in len():
        print("gia tri cua a la:",i)
        print("gia tri cua a la:",i)
        print("gia tri cua a la:",i)

    '''Trả về list N bộ integer (a, b, c) là độ dài 3 cạnh của tam giác vuông
    cạnh huyền `c` có chu vi 24 cm (perimeter), biết độ dài các cạnh <= 10cm.

    Yêu cầu dùng list comprehension.
    '''
    # result = None
    #
    # # Xoá dòng sau và viết code vào đây set các giá trị phù hợp
    # raise NotImplementedError("Học viên chưa làm bài này")
    #
    # return result


def main():
    ketqua = []

#     print(solve())
#
#
# if __name__ == "__main__":
#     main()
